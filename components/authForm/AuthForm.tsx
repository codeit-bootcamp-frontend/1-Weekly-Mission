import React, { ReactNode } from "react";
import * as S from "@/components/authForm/AuthForm.style";

// const AuthForm = ({ children }: { children: ReactNode }) => {
//   return <S.AuthForm>{children}</S.AuthForm>;
// };

// export default AuthForm;
