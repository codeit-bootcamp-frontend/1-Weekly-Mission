import React from "react";

const Card = () => {
  return (
    <div
      style={{
        fontSize: "2rem",
        width: "70rem",
        height: "50rem",
        backgroundColor: "#bada55",
        alignItems: "center",
        justifyContent: "center",
        display: "flex",
      }}
    >
      카드 컴포넌트 영역 페어 프로그래밍
    </div>
  );
};

export default Card;
