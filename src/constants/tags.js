const formEl = document.querySelector(".form");
const emailInputEl = document.querySelector("#email");
const emailErrorMessageEl = document.querySelector("#email-error");
const passwordErrorMessagesEl = document.querySelectorAll("#password-error");
const passwordInputsEl = document.querySelectorAll(".password");
const eyeButtonsEl = document.querySelectorAll(".items__eye-button");
const eyeIconsEl = document.querySelectorAll(".eye-button");

export {
  formEl,
  emailInputEl,
  emailErrorMessageEl,
  passwordErrorMessagesEl,
  passwordInputsEl,
  eyeButtonsEl,
  eyeIconsEl,
};
