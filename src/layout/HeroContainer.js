import { styled } from "styled-components";

const HeroContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 15rem 32rem 9rem;
  background-color: var(--linkbrary-bg);
`;
export default HeroContainer;
