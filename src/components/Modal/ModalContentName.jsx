import * as Styled from "./StyledModal";

const ModalContentName = ({ contentName }) => {
  return <Styled.ModalContentName>{contentName}</Styled.ModalContentName>;
};

export default ModalContentName;
