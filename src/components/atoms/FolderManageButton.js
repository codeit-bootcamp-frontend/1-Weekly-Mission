import styled from "styled-components";

const FolderManageButton = styled.div`
  display: flex;
  align-items: center;
  gap: 0.4rem;
  color: var(--linkbrary-gray-60, #9fa6b2);
  font-size: 1.4rem;
  font-weight: 600;
  cursor: pointer;
`;

export default FolderManageButton;
