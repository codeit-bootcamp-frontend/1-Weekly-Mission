const SearchBar = () => {
  return (
    <form>
      <input
        id="search"
        name="search"
        type="search"
        placeholder="링크를 검색해 보세요."
      />
    </form>
  );
};

export default SearchBar;
